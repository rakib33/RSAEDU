@model IEnumerable<RSAEDU.Models.ExamTypeInfo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ExamTypeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EntryBy)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EntryDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ExamTypeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EntryBy)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EntryDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ExamTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ExamTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ExamTypeId })
        </td>
    </tr>
}

</table>
